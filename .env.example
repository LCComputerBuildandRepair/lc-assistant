# Copy to .env and fill in. Never commit the real .env.

DATABASE_URL="file:./dev.db"
OWNER_PASSWORD="choose-a-password"
SESSION_SECRET="run: node -e \"console.log(require('crypto').randomBytes(24).toString('hex'))\""
ANTHROPIC_API_KEY="sk-ant-..."

# Notifications (contact form → text + email you)
OWNER_EMAIL="you@example.com"
OWNER_PHONE="+1..."
RESEND_API_KEY=""
NOTIFY_FROM_EMAIL="alerts@yourdomain.com"
SMS_PROVIDER="none"          # twilio | carrier | none
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM_NUMBER=""
OWNER_CARRIER_GATEWAY=""     # e.g. vtext.com (Verizon), txt.att.net (AT&T)
ALLOWED_ORIGIN="*"
